<?php

declare(strict_types=1);

/**
 * Sample migration stub — discovered by `bin/console migrate:discover`
 * (a symfony/finder demonstration). A real migration runner would execute
 * the returned closure; here schema creation lives in `install:db`, so this
 * is an intentional no-op that just gives the Finder something to find.
 */

return static function (): void {
    // no-op: demo schema is created by `install:db`
};
